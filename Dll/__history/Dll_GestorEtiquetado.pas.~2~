unit Dll_GestorEtiquetado;

interface

function REPINTAR(Tabla: AnsiString): boolean; stdcall;

implementation

function REPINTAR(Tabla: AnsiString): boolean; stdcall;
BEGIN
  if Tabla = 'Caberegu' then
    result := True;

END;

end.
